import { HideRecentPipe } from './hide-recent.pipe';

describe('HideRecentPipe', () => {
  it('create an instance', () => {
    const pipe = new HideRecentPipe();
    expect(pipe).toBeTruthy();
  });
});
